environment:
    matrix:
    # node.js
    - nodejs_version: "22"
    - nodejs_version: "24"

platform:
    - x86
    - x64

install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - npm install

build_script:
  - echo "No build step required for this Node.js project"

test_script:
  - npm run test-ci